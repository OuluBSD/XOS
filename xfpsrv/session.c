//=======================================================
// SESSION.C - XFP file server session managment routines
// Written by John Goltz
//=======================================================

#include "xfpsrv.h"


void funcconnect(
	CDB   *cdb,
	uchar *msg,
	int    len)

{
	cdb = cdb;
	msg = msg;
	len = len;
}


void funcpassowrd(
	CDB   *cdb,
	uchar *msg,
	int    len)

{
	cdb = cdb;
	msg = msg;
	len = len;
}


void funcdisconnect(
	CDB   *cdb,
	uchar *msg,
	int    len)

{
	cdb = cdb;
	msg = msg;
	len = len;
}


void funcsession(
	CDB   *cdb,
	uchar *msg,
	int    len)

{
	cdb = cdb;
	msg = msg;
	len = len;
}


void funckeepalive(
	CDB   *cdb,
	uchar *msg,
	int    len)

{
	cdb = cdb;
	msg = msg;
	len = len;
}
